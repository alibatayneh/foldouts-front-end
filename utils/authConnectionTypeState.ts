// These are the auth connection types currently supported
// which enable different login flows for different user types.
export const authConnectionTypes = {
    email: "Username-Password-Authentication",
    sms:  "sms"
};
